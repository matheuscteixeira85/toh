package br.ufs.esii.toh.controller;





public class CardapioController {
/*
	@PersistenceContext
	private EntityManagerFactory emf = Persistence.createEntityManagerFactory("TOHJPA");
	private EntityManager em = emf.createEntityManager();
	
	public void cardapioController(int cpf, String nome, String tipo_usuario, String senha) {
		
		Date data = new Date(2022,10,19);
		Administrador adm = new Administrador();
		adm.cadastrarAdministrador(cpf, nome, tipo_usuario, senha, data, data);
		
		
		em.getTransaction().begin();
		em.persist(adm);
		em.getTransaction().commit();
		
		em.close();
		emf.close();
		
		
	}
	*/
}
